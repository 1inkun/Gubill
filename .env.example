# 配置说明
#
# 本项目遵循上游约定：cmd/main/config.yaml 为唯一配置源，
# 启动时由 config 包读取并写入环境变量（含 JWT.Salt、SinglePrice、Pay.ExpireMinutes 等）。
# 因此除 CONFIG_PATH 外，无需（也无法）通过环境变量覆盖配置。
#
# 如需指定其他配置文件路径：
CONFIG_PATH=config.yaml
