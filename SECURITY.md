# 安全策略

Gubill 当前定位为**学习项目**，**尚未接入任何真实支付渠道**（支付网关接口已预留）。请在投入生产前自行完成支付渠道接入与安全加固。

## 已知安全注意事项

- 未接入支付渠道前，结算接口会返回"支付网关未配置"；管理端确认收款/退款为线下人工操作路径。
- 接入真实支付后，渠道异步回调必须严格验签（签名、金额、状态），防止伪造回调。
- `JWT_SALT` 必须通过环境变量注入，切勿使用默认值或提交到仓库。
- 管理表单当前依赖 SameSite=Lax Cookie 缓解 CSRF，生产环境建议补充 CSRF Token 与 HTTPS。
- 接入反向代理时需正确配置 Gin 的可信代理，避免限流被伪造 IP 绕过。

## 报告漏洞

如果发现安全漏洞，**请勿创建公开 Issue**，而是私下联系维护者（GitHub 主页可见联系方式），并提供：

- 漏洞描述与复现步骤
- 受影响版本
- 可选的修复建议

维护者会尽快确认并修复；修复发布前请对漏洞细节保密。

## 安全更新

安全修复会以新版本形式发布，并记录在 [CHANGELOG.md](CHANGELOG.md)。
