# 变更日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的版本化思路，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 计划中

- 接入真实支付渠道（微信/支付宝）
- 会员套餐时长字段与支付成功后自动开通
- 生产安全加固（HTTPS、CSRF Token、请求超时等）

### 变更

- 回退移除模拟支付网关（MockGateway）与 mock 回调、站内模拟支付页，保留统一支付状态机与 `Gateway` 接口，并标注真实渠道接入位置（`TODO(支付接入)`）。

## [0.2.0] - 2026-08-17

### 新增

- 可替换支付网关抽象（`internal/payment.Gateway`）与 Mock 实现（HMAC 签名回调）
- 完整支付状态机：待支付/已支付/已作废/已退款，支付成功后自动联动签到与会员订单
- 支付单过期自动作废（定时任务 + 惰性检查），过期后支持直接重新结算
- 用户端支付记录查询
- 管理端支付记录接口（确认收款/全额退款）
- CLI 命令 `create-admin` 创建/升级管理员
- Docker 多阶段构建、docker-compose、SQLite 备份脚本
- 环境变量配置体系（JWT 密钥外置）与 `.env.example`

### 修复

- 配置机制按上游约定恢复：`config.yaml` 为唯一配置源（config 包读取后写入环境变量）
- 支付单查询未排序导致可能命中作废单的问题
- 支付过期后无法自助重新结算的问题
- 管理端重置签到状态时残留结束时间与金额的问题
- 分页参数行为不一致、注册成功文案错误、限流器内存增长等问题
- SQLite 驱动切换为纯 Go 实现，本地与容器构建无需 C 编译器

### 测试

- 新增支付状态机、HMAC 签名、结算/重新结算、接口权限与可见性、管理页鉴权等测试

## [0.1.0] - 2026-08-16

### 新增

- 用户注册/登录（JWT + bcrypt）
- 签到计时与按时长阶梯计费结算
- 会员计划、会员订单、会员状态管理
- 用户端与管理端 REST API
- 统一响应/错误模型与限流中间件
